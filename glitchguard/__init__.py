"""GlitchGuard - catch real pricing mistakes before they vanish."""

# Compared against the tag of the latest GitHub release by the update checker,
# so it must follow the same "1.2.3" shape the release tags use.
__version__ = "1.0.1"
