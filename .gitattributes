# Shell scripts must keep LF endings. A .command file checked out with CRLF
# fails on macOS with a "bad interpreter" error that gives no hint why.
*.command text eol=lf
*.sh      text eol=lf

# Batch files are the opposite case and want CRLF.
*.bat     text eol=crlf
